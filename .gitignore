# 実行環境。micromamba と npm で作り直せる
.venv/
.pg/
.node/
web/node_modules/

# 稼働中のデータベースと控え。中身は成果物であってソースではない
pgdata/
db/backups/

# 学習データと学習結果。DB と Markdown から作り直せる
tuning/data/
tuning/data-labeled/
tuning/runs/

__pycache__/
*.pyc
.DS_Store

# 資格情報。GitHub の PAT をここに置く
.env

# LLM の接続先の設定とキー。正は ~/.config/justic/ に置く (docs/design-05-llm-endpoints.md の 5.5)。
# リポジトリの中に置いてしまった人を助けるための保険
endpoints.json
*-key
